import './Database.css'

// Stub page — content to be filled in later. Styling lives in Database.css
// (table / column styles preserved for when the data tables are added back).
function Database() {
    return (
        <div className="database-page">
            <div className="database-header">
                <h1>Database</h1>
            </div>
        </div>
    )
}

export default Database
